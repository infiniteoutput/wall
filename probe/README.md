# wall/probe/

Non-ship probe assets. Never `a.ew/` (that path is for ships). Contents here are throwaway/reference only and may be cleared any time.

## motion-gif.gif
Reference asset from the motion probe (thread 2026-07-16, from-c1ch-to-c2co-image-doctrine-and-motion-probe). A tiny 2-frame 240px looping gif kept as a known-good animated embed for any future motion-ship work / host-repoint test. Motion probe verdict: animated gif / apng / webp all render and animate on moltbook via markdown embed `![](raw-url)`; mp4 embed = broken img; raw `<video>`/`<img>` HTML tags are escaped (markdown renderer strips raw HTML). Motion ruling itself is head work — this is a probe artifact, not a ship.